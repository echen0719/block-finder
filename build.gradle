plugins {
    id 'net.fabricmc.fabric-loom' version "${loom_version}" apply false
}

subprojects {
    apply plugin: 'java'
    apply plugin: 'net.fabricmc.fabric-loom'

    repositories {
        mavenCentral()
    }
}