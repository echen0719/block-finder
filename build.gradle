plugins {
    id "fabric-loom" version "${loom_version}" apply false
    id "net.minecraftforge.gradle" version "[6.0,7.0)" apply false
}

subprojects {
    apply plugin: "java"

    repositories {
        mavenCentral()
    }
}